"use client";
import { FC, ReactElement } from "react";

export const DashboardReservationUpdateModule: FC = (): ReactElement => {
  return (
    <section className="flex w-full h-full">
      <h1>Reservation Complete Module</h1>
    </section>
  );
};
